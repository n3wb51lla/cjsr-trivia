{
  "rules": {
    ".read": false,
    ".write": false,
    "games": {
      "$gameCode": {
        ".read": true,
        "meta": {
          ".read": true,
          ".write": "newData.exists() && newData.child('phase').isString() && (newData.child('phase').val() === 'lobby' || newData.child('phase').val() === 'question' || newData.child('phase').val() === 'reveal' || newData.child('phase').val() === 'break' || newData.child('phase').val() === 'final' || newData.child('phase').val() === 'sudden_death') && (!newData.child('currentQuestionIndex').exists() || newData.child('currentQuestionIndex').isNumber() || newData.child('currentQuestionIndex').val() === null) && (!newData.child('currentRound').exists() || newData.child('currentRound').isNumber() || newData.child('currentRound').val() === 'suddenDeath' || newData.child('currentRound').val() === null) && (!newData.child('questionStartedAt').exists() || newData.child('questionStartedAt').isNumber() || newData.child('questionStartedAt').val() === null)"
        },
        "teamNames": {
          "$teamNameKey": {
            ".read": true,
            ".write": "newData.val() === null || (!data.exists() && newData.isString())"
          }
        },
        "teams": {
          ".read": true,
          "$teamId": {
            ".write": "newData.exists() && newData.child('teamName').isString() && newData.child('teamName').val().length > 0 && newData.child('teamName').val().length <= 40 && newData.child('playerCount').isNumber() && newData.child('playerCount').val() >= 1 && newData.child('playerCount').val() <= 20 && newData.child('score').isNumber() && newData.child('score').val() >= 0 && newData.child('cumulativeLockMs').isNumber() && newData.child('cumulativeLockMs').val() >= 0 && newData.child('isActive').isBoolean()"
          }
        },
        "answers": {
          ".read": true,
          ".write": "newData.val() === null",
          "$teamId": {
            "$questionId": {
              ".write": "newData.exists() && root.child('games/' + $gameCode + '/teams/' + $teamId).exists() && newData.child('teamId').val() === $teamId && newData.child('questionIndex').isNumber() && newData.child('questionIndex').val() >= 1 && newData.child('questionIndex').val() <= 1000 && (newData.child('choiceIndex').val() === null || (newData.child('choiceIndex').isNumber() && newData.child('choiceIndex').val() >= 0 && newData.child('choiceIndex').val() <= 3)) && (newData.child('choiceIndexes').val() === null || newData.child('choiceIndexes').numChildren() <= 4) && (newData.child('textAnswer').val() === null || (newData.child('textAnswer').isString() && newData.child('textAnswer').val().length <= 200)) && newData.child('pointsAwarded').isNumber() && newData.child('pointsAwarded').val() >= 0"
            }
          }
        },
        "questions": {
          "$questionId": {
            ".write": "newData.exists() && newData.child('id').isNumber() && (newData.child('round').val() === 'suddenDeath' || (newData.child('round').isNumber() && newData.child('round').val() >= 1 && newData.child('round').val() <= 200)) && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 140 && (newData.child('type').val() === 'free_text' ? (newData.child('acceptedAnswers').numChildren() >= 1 && newData.child('acceptedAnswers').numChildren() <= 10) : (newData.child('choices').hasChildren(['0','1','2','3']) && (newData.child('type').val() === 'multi_select' ? (newData.child('answers').numChildren() >= 1 && newData.child('answers').numChildren() <= 4) : (newData.child('type').val() === 'multiple_choice' && newData.child('answer').isNumber() && newData.child('answer').val() >= 0 && newData.child('answer').val() <= 3)))) && (!newData.child('media').exists() || (newData.child('media').child('type').isString() && (newData.child('media').child('type').val() === 'image' || newData.child('media').child('type').val() === 'video') && newData.child('media').child('url').isString() && newData.child('media').child('url').val().length > 0 && newData.child('media').child('url').val().length <= 2048 && newData.child('media').child('altText').isString() && newData.child('media').child('altText').val().length > 0 && newData.child('media').child('altText').val().length <= 250 && (newData.child('media').child('captionsUrl').val() === null || (newData.child('media').child('captionsUrl').isString() && newData.child('media').child('captionsUrl').val().length <= 2048))))"
          }
        }
      }
    }
  }
}
