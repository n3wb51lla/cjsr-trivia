Uppercase mono kicker that sits above section headings for rhythm and context.

```jsx
<Eyebrow>Trivia night software built for hosts</Eyebrow>
<Eyebrow onDark rule={false}>Pricing</Eyebrow>
```

Set `onDark` on navy sections; set `rule={false}` to drop the short leading dash.
