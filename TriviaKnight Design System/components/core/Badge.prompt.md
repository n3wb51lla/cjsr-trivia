Small uppercase pill for labels and status markers such as plan tiers and premium-feature flags.

```jsx
<Badge tone="gold">Most Popular</Badge>
<Badge tone="soft">Pro Host</Badge>
```

Tones: `gold`, `navy`, `outline`, `success`, `soft`. Pass `onDark` for the outline tone on navy backgrounds, and `icon` for a leading Lucide glyph.
