Labelled native select styled to match the form fields. Use for the contact form's "Primary use case".

```jsx
<Select label="Primary use case" required placeholder="Select one"
  options={["Independent trivia host","Bar, restaurant, or brewery","Corporate event"]} />
```

Accepts string options or `{value,label}` objects.
