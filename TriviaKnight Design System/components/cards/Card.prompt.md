Generic rounded, softly-elevated surface; compose feature, pricing, and use-case content inside it.

```jsx
<Card hover>...</Card>
<Card tone="dark" padding="lg">...</Card>
```

`tone`: `light` / `dark`. `hover` adds the lift interaction, `flat` removes the shadow, `padding="lg"` for roomier cards.
